services:
  kr-apartment-market:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    ports:
      - "8765:8765"
    volumes:
      - ./.data:/app/.data
    restart: unless-stopped
