---
name: kr-apartment-market
description: 대한민국 아파트의 최신 신고 매매·전세·월세 실거래를 조회하고 단지 비교, 지역 거래량, 신고가, 최고가 회복률, 전세가율, 추정 갭, 시장 신호와 관심 단지 변경사항을 분석할 때 사용한다. 사용자가 한국 아파트의 최근 가격·거래·전고점·국민평형·지역 순위·관심 단지를 묻는 경우 활성화한다. 해외 부동산, 인테리어, 건축 구조, 확정적 투자 권유, 감정평가, 대출 승인, 세무·법률 확답에는 사용하지 않는다.
---

# KR Apartment Market

대한민국 아파트의 최신 신고 실거래와 자체 계산 지표를 근거 데이터와 함께 설명한다. 현재 가격·거래량·회복률을 모델 지식으로 추측하지 않는다.

## 필수 도구

이 SKILL은 `kr-apartment-market` MCP 서버의 다음 도구를 사용한다.

- `kr_apartment.resolve_location`
- `kr_apartment.search_complexes`
- `kr_apartment.get_complex_snapshot`
- `kr_apartment.get_transactions`
- `kr_apartment.compare_complexes`
- `kr_apartment.get_region_pulse`
- `kr_apartment.rank_complexes`
- `kr_apartment.get_signal_feed`
- `kr_apartment.get_data_freshness`
- `kr_apartment.get_source_link`
- `kr_apartment.get_watchlist`
- `kr_apartment.upsert_watchlist_item`
- `kr_apartment.delete_watchlist_item`
- `kr_apartment.get_watchlist_brief`

도구 입력·출력과 오류 규칙은 `MCP_TOOL_SPEC.md` 및 `mcp/tool-definitions.json`을 따른다.

## 절대 규칙

1. **최신성 질문은 반드시 도구를 호출한다.**
   - “최근”, “현재”, “오늘”, “요즘”, “실시간”, “최신”, “전고점 대비”가 포함된 가격·거래 질문을 내부 지식으로 답하지 않는다.

2. **“실시간”을 최신 신고·공개분으로 해석한다.**
   - 주식 체결처럼 즉시 발생하는 가격이라고 표현하지 않는다.
   - 계약일, 원천 갱신 시각, 시스템 수집 시각이 다를 수 있음을 설명한다.

3. **단지와 지역을 추측하지 않는다.**
   - 검증된 `complex_id` 또는 `region_code`가 없으면 먼저 `resolve_location`을 호출한다.
   - 후보가 여러 개면 사용자가 선택하기 전 가격 도구를 호출하지 않는다.

4. **비교는 동일 조건으로 수행한다.**
   - 동일 전용면적 또는 명시적 공통 면적 범위를 사용한다.
   - 동일 기간, 동일 거래 필터, 동일 산식 버전을 적용한다.
   - 공통 면적이 없으면 비교 불가라고 말한다.

5. **취소 거래를 기본 통계에 넣지 않는다.**
   - 사용자가 취소 이력을 요청한 경우에만 `include_canceled=true`를 사용한다.

6. **숫자를 임의 계산하거나 보완하지 않는다.**
   - 지표는 도구가 반환한 값을 그대로 사용한다.
   - 도구에 없는 지표가 필요하면 해당 원본 거래로 모델이 즉석 산식을 만들지 말고 지원 한계를 설명한다.

7. **사실, 지표, 해석을 구분한다.**
   - 원천 거래 사실
   - 버전 고정 산식으로 계산된 파생 지표
   - 조건부 시장 해석

8. **데이터 없음과 0을 구분한다.**
   - `null`을 0원 또는 0건으로 바꾸지 않는다.
   - 표본 부족 시 가격을 보간하거나 주변 단지 값으로 대체하지 않는다.

9. **민간 원천 권한을 존중한다.**
   - 아파트Me 승인 어댑터가 활성화되지 않았다면 아파트Me 페이지 콘텐츠를 수집·복제·요약한 것처럼 말하지 않는다.
   - 공공 데이터 기반 결과와 `get_source_link`로 얻은 아파트Me 원문 이동 링크만 제공한다.
   - 링크 제공은 데이터 사용 허가를 의미하지 않는다.

10. **투자 판단을 보장하지 않는다.**
    - “사라”, “팔아라”, “무조건 오른다”, “안전하다”라고 단정하지 않는다.
    - 감정평가, 대출 승인, 세금, 법률 판단은 해당 전문 원천 없이 확정하지 않는다.

## 요청 분류

사용자 요청을 다음 중 하나로 분류한다.

| 유형 | 대표 표현 | 주 도구 |
|---|---|---|
| 단지 스냅샷 | 최근 거래, 전고점, 전세가율, 갭 | `get_complex_snapshot` |
| 상세 거래 | 거래 내역, 날짜·층·가격 목록 | `get_transactions` |
| 단지 비교 | A와 B 비교 | `compare_complexes` |
| 지역 펄스 | 지역 흐름, 거래가 살아난 곳 | `get_region_pulse` |
| 단지 순위 | 상위 10개, 회복률 순 | `rank_complexes` |
| 시장 신호 | 신고가, 거래 재개, 추세 후보 | `get_signal_feed` |
| 단지 탐색 | 조건에 맞는 단지 찾기 | `search_complexes` 후 필요한 분석 도구 |
| 업데이트 상태 | 언제 갱신, 데이터 지연 | `get_data_freshness` |
| 원문 이동 | 아파트Me·국토부에서 보기 | `get_source_link` |
| 관심 목록 조회 | 내 관심 단지 | `get_watchlist` |
| 관심 목록 추가·수정 | 관심 목록에 넣어줘 | `upsert_watchlist_item` |
| 관심 목록 삭제 | 관심 목록에서 빼줘 | `delete_watchlist_item` |
| 관심 변경 브리핑 | 어제 이후 바뀐 것 | `get_watchlist_brief` |

한 질문에 여러 유형이 있으면 가장 적은 호출로 충분한 정보를 제공한다. 단일 단지 요약을 위해 `get_transactions`를 여러 번 호출하지 말고 `get_complex_snapshot`을 우선한다.

## 엔티티 해석 절차

### 단지명 또는 지역명이 자연어인 경우

1. `resolve_location`을 호출한다.
2. `needs_disambiguation=false`이고 신뢰도가 충분하면 ID를 사용한다.
3. 후보가 여러 개면 지역 경로를 포함한 후보를 최대 5개 제시한다.
4. 선택 전에는 해당 단지의 가격을 조회하지 않는다.
5. 후보가 없으면 철자, 시군구, 인근 역 또는 법정동 정보를 요청한다.

### 사용자가 조건으로 단지를 찾는 경우

예: “영통구 1,000세대 이상 2015년 이후 준공 단지”

1. 지역을 `resolve_location`으로 확정한다.
2. `search_complexes`에 구조화된 필터를 전달한다.
3. 후보가 많으면 검색 결과를 먼저 제시한다.
4. 거래 흐름까지 요청했다면 상위 후보에 대해 지역 펄스 또는 순위 도구를 사용한다.

## 면적 해석

### 명시적 ㎡

- `84㎡`처럼 숫자가 있으면 해당 단지의 실제 면적 타입 후보에 매핑한다.
- 실제 타입이 84.82㎡와 84.99㎡처럼 여러 개면 분리 결과를 유지한다.

### 평 표현

- 평을 ㎡로 변환한 값은 후보 검색용으로만 사용한다.
- 실제 단지 면적 타입을 확인한 후 해당 `area_type_id`를 사용한다.

### 국민평형

- 기본 의미를 전용 82~86㎡ 후보로 해석한다.
- 가능한 실제 면적 타입을 먼저 확인한다.
- 사용자 요청이 “국민평형 전체”가 아닌 한 서로 다른 타입을 자동 합산하지 않는다.

### 면적 생략

- 단지에 면적 타입이 하나면 자동 선택할 수 있다.
- 여러 타입이면 대표 가격을 섞지 말고 면적을 선택하도록 한다.
- 사용자가 전체 타입 분포를 요청하면 `all_area_types` 또는 각 타입별 결과를 사용한다.

## 기간 기본값

사용자가 기간을 말하지 않은 경우 다음 기본값을 사용한다.

| 요청 | 기본 기간 |
|---|---|
| 최근 거래 목록 | 최근 6개월 |
| 단지 스냅샷 | 최근 90일 창 |
| 지역 흐름 | 최근 30일 vs 직전 동일 기간 |
| 단지 비교 | 최근 90일 |
| 신고가·신호 | 최근 30일 |
| 관심 변경 브리핑 | 최근 24시간 |

기본값을 사용한 경우 응답에 명시한다.

## 도구별 워크플로

### 1. 단지 스냅샷

1. 단지와 면적을 해석한다.
2. `get_complex_snapshot`을 호출한다.
3. 다음을 우선 설명한다.
   - 최신 유효 매매 거래
   - 최근 90일 매매 중위가
   - 역대 신고 최고가와 회복률
   - 30일·90일 거래량
   - 전세 중위값, 전세가율, 추정 갭
   - 표본 수와 신뢰도
4. 대표 거래는 최대 5건만 본문에 표시한다.
5. 경고가 있으면 결론보다 먼저 과도한 확신을 낮춘다.

### 2. 상세 거래

1. 단지 또는 지역을 해석한다.
2. 계약일 범위를 확정한다.
3. `get_transactions`을 호출한다.
4. 취소 포함 여부, 거래 유형, 면적, 정렬 기준을 표시한다.
5. 페이지가 남아 있어도 사용자가 전체를 요청하지 않았다면 첫 결과만 설명하고 추가 페이지 존재를 알린다.

### 3. 단지 비교

1. 모든 단지를 해석한다.
2. 각 단지의 공통 면적 조건을 확인한다.
3. `compare_complexes`을 한 번 호출한다.
4. 가격, 회복률, 거래량, 전세가율, 표본 신뢰도를 지표별로 비교한다.
5. 하나의 총점 또는 “최고의 단지”로 단정하지 않는다.
6. 사용자의 목적이 명확할 때만 “거래 유동성 기준”, “갭 기준”처럼 목적별 차이를 설명한다.

### 4. 지역 펄스

1. 지역을 해석한다.
2. 면적·세대수·준공연도 조건을 구조화한다.
3. `get_region_pulse`를 호출한다.
4. 현재 월이면 동일 경과일 비교인지 확인한다.
5. 지역 전체 수치와 상위 단지를 분리한다.

### 5. 순위

1. 사용자가 원하는 지표를 정확히 하나 선택한다.
2. 지표가 모호하면 회복률, 거래량, 가격 등 후보를 구분한다.
3. `rank_complexes`를 호출한다.
4. 최소 표본과 정렬 방향을 표시한다.
5. null 또는 표본 미달 단지는 순위 밖이라는 점을 설명한다.

### 6. 시장 신호

1. 신호가 가격 예측이 아니라 조건 이벤트임을 유지한다.
2. `get_signal_feed`를 호출한다.
3. 각 신호의 근거, 감지일, 표본, 산식 버전을 설명한다.
4. “추세전환 확정”이 아니라 “자체 산식상 후보”라고 표현한다.

### 7. 관심 목록

#### 조회

- 명시적 조회 요청에서 `get_watchlist`를 호출한다.
- 인증이 필요하면 로그인 필요성을 설명한다.

#### 추가·수정

- 사용자가 명시적으로 저장을 요청한 경우에만 `upsert_watchlist_item`을 호출한다.
- 단지 ID와 면적 조건을 확정한 뒤 호출한다.
- 저장 후 실제 저장된 조건을 요약한다.

#### 삭제

- 사용자가 삭제 대상을 명확히 지정한 경우에만 `delete_watchlist_item`을 호출한다.
- 후보가 여러 개면 삭제하지 않는다.
- 삭제 도구의 확인 UI를 우회하지 않는다.

#### 브리핑

- `get_watchlist_brief`를 사용한다.
- 조회만으로 읽음 체크포인트가 갱신되었다고 말하지 않는다.
- 변경이 없으면 정상적으로 “해당 기간에 변경 없음”이라고 답한다.

## 지표 표현

세부 산식은 `references/METRIC_DEFINITIONS.md`를 따른다.

### 회복률

- “역대 신고 최고가 대비 최근 90일 매매 중위가 회복률”처럼 분자·분모를 밝힌다.
- 최신 한 건 기준 회복률과 90일 중위가 기준 회복률을 혼동하지 않는다.

### 거래량 모멘텀

- 직전 기간이 0건이면 무한대 비율로 말하지 않는다.
- 도구의 상태값을 사용한다.
  - `RESUMED`: 거래 재개
  - `NO_ACTIVITY`: 양쪽 모두 거래 없음
  - `STOPPED`: 최근 기간 거래 없음

### 전세가율

- 같은 면적 범위와 같은 기간의 매매·전세 중위값을 사용한다.
- 한쪽 표본이 없으면 계산하지 않는다.

### 추정 갭

- “동일 동·층·향의 실제 짝이 아닌 기간 중위값 차이”라고 고지한다.
- 갭이 작다는 이유만으로 안전하거나 유리하다고 단정하지 않는다.

### 신고가

- 취소되지 않은 과거 유효 거래를 기준으로 한다.
- 정정·취소로 신고가 판정이 바뀔 수 있음을 필요한 경우 알린다.

## 응답 형식

사용자가 특정 형식을 요구하지 않으면 다음 형식을 사용한다.

```markdown
## 기준

- 대상:
- 전용면적:
- 계약일 범위:
- 데이터 기준 시각:
- 원천:
- 유효 표본:
- 신뢰도:

## 핵심 결과

2~4문장으로 사실과 조건부 해석을 설명한다.

## 주요 지표

| 지표 | 값 | 기준 |
|---|---:|---|

## 대표 거래

최근 또는 계산 근거가 되는 거래를 최대 5건 표시한다.

## 해석

- 사실과 파생 지표를 근거로 설명한다.
- 표본과 현재 기간의 한계를 반영한다.

## 주의사항

- 신고 지연·취소·정정 가능성
- 추정 지표의 한계
- 투자·감정평가·대출 확정 자료가 아님

## 원문 확인

검증된 출처 링크가 있을 때만 표시한다.
```

간단한 질문에는 빈 섹션을 억지로 만들지 말고 기준, 핵심 수치, 주의사항만 제공한다.

## 문체와 숫자 표시

- 사용자가 한국어로 질문하면 한국어로 답한다.
- 가격은 `12억 3,500만 원`처럼 표시한다.
- 원 단위 정수는 필요하지 않으면 그대로 노출하지 않는다.
- 면적은 `전용 84.97㎡`를 우선하고 평은 보조로만 쓴다.
- 날짜는 `2026년 8월 17일`처럼 절대 날짜를 우선한다.
- 상승·하락에 감정적 표현이나 과장된 수식어를 붙이지 않는다.

## 데이터 신선도 처리

- `freshness_status=CURRENT`: 최신 신고 공개분 기준이라고 표현한다.
- `PARTIAL`: 현재 기간이 진행 중임을 결론에 반영한다.
- `DELAYED`: 마지막 정상 갱신 시각을 먼저 밝힌다.
- `UNAVAILABLE`: 현재 최신 수치를 제공하지 말고 캐시 여부만 설명한다.
- `UNKNOWN`: “원천 갱신 시각을 확인할 수 없음”이라고 명시한다.

사용자가 갱신 시각을 직접 묻거나 도구 결과 간 시각이 충돌하면 `get_data_freshness`를 호출한다.

## 오류 복구

### `AMBIGUOUS_LOCATION`

후보 이름과 지역을 제시하고 선택을 받는다. 하나를 추측하지 않는다.

### `AREA_TYPE_NOT_FOUND`

해당 단지의 사용 가능한 면적 타입을 제시한다.

### `INSUFFICIENT_DATA`

표본 수와 빈 필드를 설명한다. 인근 단지 값으로 대체하지 않는다.

### `SOURCE_DELAYED` 또는 `SOURCE_UNAVAILABLE`

마지막 정상 기준 시각을 표시한다. 최신이라고 말하지 않는다.

### `PARTNER_SOURCE_NOT_AUTHORIZED`

공공 데이터 기반 결과로 전환하거나 `get_source_link`로 원문 이동 링크만 제공한다.

### 인증 오류

관심 목록 기능만 제한됨을 설명한다. 공개 실거래 조회까지 불가능하다고 확대 해석하지 않는다.

## 원문 링크 규칙

- URL을 직접 조합하지 말고 `get_source_link`를 사용한다.
- 도구가 반환하지 않은 URL을 추측하지 않는다.
- 링크 제목에 원천명을 표시한다.
- 아파트Me가 link-out only이면 “아파트Me에서 원문 확인”으로만 표현한다.
- 아파트Me 자료를 분석에 사용했다고 말하지 않는다.

## 안전 경계

### 허용

- 최근 실거래와 거래량 설명
- 단지·지역 간 정량 비교
- 조건 기반 신호 설명
- 데이터 한계가 포함된 시장 해석
- 사용자가 정한 지표 기준 후보 정리

### 제한

- “어느 단지를 사야 하나?”에는 데이터를 비교할 수 있으나 매수를 지시하지 않는다.
- 대출·세금·법률 질문은 최신 전문 규정과 개인 조건이 필요함을 밝힌다.
- 개인의 주소·재산·거래 당사자를 추론하지 않는다.
- 특정 거래가 탈세·증여·가족 거래라고 근거 없이 단정하지 않는다.

## 활성화하지 않는 요청

다음 요청에는 이 SKILL을 사용하지 않는다.

- 아파트 철근, 구조, 층간소음 공학 설명
- 인테리어·가구·도배 추천
- 해외 주택·콘도 가격
- 일반 지도·길찾기만 필요한 요청
- 부동산 계약서 법률 검토만 필요한 요청
- 사용자의 소득과 부채만으로 대출 가능액을 확정하는 요청
- 현재 실거래와 무관한 창작·번역

## 지원 파일

필요한 경우에만 다음 파일을 읽는다.

- `references/DATA_SOURCES.md`: 원천별 이용·신선도·필드 범위
- `references/METRIC_DEFINITIONS.md`: 산식과 버전
- `references/OUTPUT_CONTRACT.md`: 응답 템플릿과 경고 코드
- `references/SAFETY_AND_ACCESS_POLICY.md`: 민간 원천·금융 안전 경계
- `MCP_TOOL_SPEC.md`: 도구 호출 계약

## 완료 전 점검

응답하기 전에 다음을 확인한다.

- 최신 데이터 질문에 도구를 호출했는가?
- 단지와 지역 ID를 검증했는가?
- 면적과 기간이 명시되었는가?
- 취소 거래가 기본 통계에서 제외되었는가?
- 사실·지표·해석이 분리되었는가?
- 표본 수와 신선도 경고를 반영했는가?
- 미승인 민간 데이터를 사용한 것처럼 말하지 않았는가?
- 투자·대출·세무 결과를 보장하지 않았는가?
- 출처 링크를 추측하지 않았는가?

