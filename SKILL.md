---
name: kr-apartment-market
description: >
  대한민국 아파트·오피스텔·연립다세대·단독주택·상업용 건물의 최신 신고
  매매·전월세 거래를 조회하고, 동일 전용면적 단지 비교, 거래량, 신고가,
  최고가 회복률, 전세가율, 추정 갭, 지역 시장 펄스, 관심 단지 브리핑,
  청약 정보와 가정 기반 금융 계산이 필요할 때 사용한다. 현재 가격을 모델
  기억으로 답하거나 미래 가격·대출 승인·투자 수익을 보장하는 용도로는 사용하지 않는다.
---

# KR Apartment Market AI Skill

## 목적

한국 부동산 질문을 원천 데이터 조회, 결정론적 계산, 제한된 해석의 순서로 처리한다. 최신성이 필요한 값은 모델 지식으로 추측하지 않는다.

## 활성화

다음 요청에서 활성화한다.

- 특정 지역·단지의 최근 매매·전세·월세 실거래
- 동일 전용면적 단지 비교
- 거래량, 신고가, 회복률, 전세가율, 추정 갭
- 지역별 순위와 거래 재개 신호
- 아파트 외 오피스텔·빌라·단독주택·상업용 건물 거래
- 청약 공고·경쟁률·당첨 결과
- 관심 단지 등록·변경 브리핑
- 대출 상환액, 복리, 임대 현금흐름의 수학적 계산

다음 요청에는 활성화하지 않는다.

- 해외 부동산
- 인테리어·건축 설계·하자 진단
- 현재 데이터가 필요 없는 일반 개념 설명
- 확정 세무·법률·대출 심사
- “무조건 오를 단지”, “지금 사야 하는 곳” 같은 보장형 요청

## 핵심 규칙

1. 최근·오늘·현재·실시간 가격에는 반드시 MCP 도구를 호출한다.
2. “실시간”은 최신 신고·공개 거래라고 설명한다.
3. 지역명이 모호하면 `kr_apartment.resolve_location`으로 후보를 확인한다.
4. 5자리 LAWD_CD를 추측해 생성하지 않는다.
5. 단지 비교는 같은 기간, 같은 면적 또는 명시적 허용 오차를 사용한다.
6. 취소 거래는 기본 통계에서 제외하되 원본 상태는 보존한다.
7. 데이터 없음과 0을 구분한다. 계산 불가는 `null` 또는 “표본 없음”이다.
8. 지표는 MCP 결과를 사용하고 대화 중 임의로 재계산하지 않는다.
9. 사실, 파생 지표, 해석을 서로 다른 문장 또는 섹션으로 구분한다.
10. 원천, 조회 월, 답변 시각, 표본 수와 품질을 표시한다.
11. 아파트Me는 승인된 어댑터가 없으면 링크아웃만 제공한다.
12. 금융 계산은 사용자가 준 가정의 산술 결과이며 승인·수익을 보장하지 않는다.

## 도구 선택

### 1. 지역 확정

사용자가 지역명만 제시했거나 동명 지역 가능성이 있으면:

```text
kr_apartment.resolve_location
```

후보가 하나로 충분히 좁혀지지 않으면 지역을 선택받는다. 단, 질문에 이미 5자리 코드와 명확한 지역이 있으면 반복 확인하지 않는다.

### 2. 원본 거래

최근 거래 목록, 특정 자산 유형, 취소 여부 확인:

```text
kr_apartment.get_transactions
```

- apartment: 아파트
- officetel: 오피스텔
- villa: 연립·다세대
- house: 단독·다가구
- commercial: 상업·업무용
- sale: 매매
- rent: 전세·월세를 함께 조회하고 `rent_type`과 월세액으로 구분

기간이 없으면 현재 월을 기본으로 하되, 추세나 최고가 질문은 충분한 과거 기간을 명시한다. 통상 12개월, 장기 최고가는 사용자가 허용한 범위에서 더 길게 조회한다.

### 3. 단지명 탐색

정확한 단지명 철자나 API 표기를 모르면:

```text
kr_apartment.search_complexes
```

검색 결과가 없는 단지를 임의로 만들어 조회하지 않는다.

### 4. 단지 스냅샷

한 단지의 매매·전세·회복률·갭:

```text
kr_apartment.get_complex_snapshot
```

`area_m2`가 없으면 여러 면적이 혼합될 수 있음을 경고한다. 사용자가 “국민평형”이라고 하면 전용 82~86㎡ 또는 구체적인 84㎡로 해석한 기준을 명시한다.

### 5. 단지 비교

```text
kr_apartment.compare_complexes
```

각 항목에 `lawd_code`, `complex_name`, `area_m2`를 넣는다. 면적이 다른 비교는 사용자 요청이 명시적인 경우에만 수행하고 왜곡 가능성을 설명한다.

### 6. 지역 흐름

```text
kr_apartment.get_region_pulse
kr_apartment.rank_complexes
kr_apartment.get_signal_feed
```

- pulse: 최근 30일 대 직전 30일
- rank: 거래량·중위가·회복률·모멘텀·전세가율·추정 갭
- signal: 신고가·거래 재개

신호를 매수·매도 신호라고 부르지 않는다.

### 7. 최신성·원문

```text
kr_apartment.get_data_freshness
kr_apartment.get_source_link
```

아파트Me를 언급할 때 승인 데이터가 없으면 `get_source_link(source="apt2me")`만 사용한다.

### 8. 관심 목록

```text
kr_apartment.get_watchlist
kr_apartment.upsert_watchlist_item
kr_apartment.delete_watchlist_item
kr_apartment.get_watchlist_brief
```

쓰기 전에 대상 지역·단지·면적을 응답에 명시한다. 삭제 결과가 false이면 성공했다고 말하지 않는다. 로컬 저장 모드는 개인용이며 공개 HTTP 다중 사용자에서는 OAuth/PostgreSQL 어댑터가 필요하다.

### 9. 금융 계산

```text
kr_apartment.calculate_loan_payment
kr_apartment.calculate_compound_growth
kr_apartment.calculate_monthly_cashflow
```

세금, 중도상환수수료, 보증료, 변동금리, 공실·수선비 등 누락 가정을 별도로 적는다. 은행 심사 결과를 추정하지 않는다.

### 10. 청약 및 upstream 호환 도구

`ENABLE_REAL_ESTATE_MCP_COMPAT=true`인 서버에서는 vendored `real-estate-mcp`의 지역, 유형별 거래, 청약홈/ODCloud, 금융 도구가 함께 노출된다.

- 청약 공고·결과는 해당 호환 도구를 사용한다.
- 같은 목적에 canonical 도구가 있으면 `kr_apartment.*`를 우선한다.
- 호환 도구의 0 기본값이나 요약 문구를 그대로 투자 판단으로 전달하지 않는다.
- canonical 응답 계약과 다른 결과는 사실·표본·원천을 다시 확인해 설명한다.

## 출력 계약

### 한눈에 보기

- 기준 시각
- 원천
- 조회 기간 또는 계약 월
- 대상 지역·단지·면적
- 유효 거래 수와 데이터 품질

### 사실

원본 거래와 집계값만 적는다.

### 파생 지표

산식 이름과 값을 적는다. 계산 불가는 이유를 적는다.

### 해석

가능성·제약·표본 한계를 포함한 제한적 해석만 제공한다.

### 주의사항

- 신고 지연, 취소, 정정 가능성
- 호가나 현재 매물 가격과 다름
- 투자·감정평가·세무·대출 확정 자료가 아님

### 원문

가능한 경우 공공 원문과 프로젝트/아파트Me 링크를 제공한다.

## 금지 표현

- “현재 시세는 정확히 …이다” — 신고 실거래 기준을 생략하지 않는다.
- “거래량이 무한대 증가했다” — 분모 0이면 거래 재개라고 한다.
- “전세가율 70% 이상이므로 위험하다” — 단일 임계값으로 단정하지 않는다.
- “이 단지는 반드시 오른다/사야 한다”
- “대출이 승인된다”
- “아파트Me 데이터로 계산했다” — 승인 어댑터가 실제 활성화되지 않았다면 금지한다.

## 실패 처리

- API 키 없음: 필요한 환경 변수와 발급 경로를 안내한다.
- 지역 모호: 후보를 제시한다.
- 원천 오류: 오류 코드를 숨기지 말고 재시도 여부를 설명한다.
- 표본 없음: 주변 단지 값으로 임의 보간하지 않는다.
- 페이지·기간 상한: 범위를 나눠 조회한다.
- 호환 계층 실패: canonical 공공데이터 도구로 가능한 범위를 계속 처리한다.
