# KR Apartment Market AI Skill v2.0 PRD

## 1. 문서 정보

| 항목 | 값 |
|---|---|
| 제품명 | KR Apartment Market AI Skill |
| 버전 | 2.0.0 |
| 상태 | Beta 구현 기준 |
| 기본 시간대 | Asia/Seoul |
| 배포 형태 | Python 패키지, stdio MCP, Streamable HTTP, Docker |
| 핵심 원천 | 국토교통부 실거래 공개 API |
| 보조 원천 | 청약홈/ODCloud, 허가된 파트너 데이터, 아파트Me 링크아웃 |
| 라이선스 | MIT + vendored upstream MIT 고지 |

## 2. 제품 정의

대한민국 부동산의 최신 신고 실거래를 AI가 직접 조회하고, 같은 기간·면적·산식으로 계산한 지표와 원천 근거를 함께 설명하도록 하는 통합 SKILL 및 MCP 서버입니다.

v2는 v1의 설계 문서와 고수준 분석 계약에 `real-estate-mcp`의 공공데이터 실행 범위를 내부 통합합니다. 사용자는 별도 MCP 저장소를 설치하거나 두 서버를 병렬 운영하지 않습니다.

## 3. 문제

- LLM은 현재 실거래가를 모델 기억만으로 정확히 답할 수 없습니다.
- 원천 API는 자산 유형별 엔드포인트와 필드명이 달라 AI가 직접 쓰기 어렵습니다.
- 취소·정정 거래, 면적 차이, 신고 지연을 무시하면 지표가 왜곡됩니다.
- 단순 거래 조회와 고수준 단지 분석이 별도 도구에 흩어지면 사용자 경험과 운영이 복잡해집니다.
- 아파트뿐 아니라 오피스텔, 빌라, 단독주택, 청약, 금융 계산도 같은 대화 안에서 필요합니다.

## 4. 목표

1. API 키만으로 DB 없이 작동하는 독립형 MCP 서버를 제공합니다.
2. 국토교통부 거래를 공통 `Transaction` 모델로 정규화합니다.
3. 17개 canonical 고수준 도구 계약을 실제 실행 코드로 연결합니다.
4. 원본 `real-estate-mcp` 범위를 저장소 내부 호환 계층으로 보존합니다.
5. 취소 거래, 페이지네이션, 재시도, 최신성, 표본 품질을 명시적으로 처리합니다.
6. 투자 권유가 아닌 근거 중심 조사·분석 기반을 제공합니다.
7. 공개 서비스로 확장할 수 있는 PostgreSQL·Redis·OAuth 설계를 유지합니다.

## 5. 비목표

- 실시간 호가·체결 스트림 제공
- 미래 가격이나 투자 수익 보장
- 감정평가, 세금, 대출 승인 확정
- 허가되지 않은 아파트Me·민간 시세 자동 수집 및 재배포
- 서로 다른 면적·기간의 거래를 숨겨서 혼합
- 사용자 확인 없이 파괴적 관심 목록 작업 실행

## 6. 사용자

### 실수요자·개인 조사자
관심 단지 최근 거래와 전세 흐름, 비교 단지의 회복률·거래량을 빠르게 확인합니다.

### 부동산 리서처·콘텐츠 제작자
지역 펄스, 신고가, 순위와 근거 레코드를 기사·브리핑·뉴스레터 입력으로 사용합니다.

### AI·MCP 개발자
ChatGPT, Codex, Claude Code 등 에이전트에 한국 부동산 조회·분석 기능을 연결합니다.

### 프롭테크·사내 분석팀
공공 API 어댑터와 지표 엔진을 상담 보조, 대시보드, 알림, 리서치 파이프라인에 재사용합니다.

## 7. 제품 모드

### Mode A: Standalone On-demand

- 필수: `DATA_GO_KR_API_KEY`
- 저장소: 불필요
- 호출 시 원천 API 조회
- 로컬 JSON 관심 목록
- 개인·개발·PoC에 적합

### Mode B: Managed Analytics

- PostgreSQL 16+, 선택적 Redis
- 전국 또는 관심 지역 증분 수집
- 거래 revision, 품질 이슈, 지표 snapshot 저장
- OAuth 관심 목록과 RLS
- 공개 서비스·사내 다중 사용자에 적합

### Mode C: Authorized Partner Extension

- 서면 허가된 Apt2Me 또는 민간 원천만 활성화
- 허용 필드·캐시 기간·재배포 범위를 데이터 원천 레지스트리에 기록
- 기본 배포에서는 비활성화

## 8. 기능 요구사항

### FR-001 지역 해석

- 한국어 지역명, 5자리 LAWD_CD, 10자리 법정동 코드를 후보로 해석합니다.
- 동일 점수의 다른 지역 후보가 있으면 자동 확정하지 않습니다.
- 지역 코드 리소스는 패키지에 포함해 네트워크 없이 검색할 수 있어야 합니다.

### FR-002 통합 거래 조회

- 자산 유형: apartment, officetel, villa, house, commercial
- 거래 유형: sale, rent
- 기간: YYYYMM, YYYY-MM, YYYY-MM-DD
- 필터: 단지명, 면적, 면적 범위, 취소 포함 여부
- 결과: 표준 거래 모델, 원천, 조회 월, 수집 시각, 총 일치 건수

### FR-003 API 안정성

- `totalCount`에 따라 모든 페이지를 순회합니다.
- 429와 일시적 5xx, 네트워크 오류에 제한적 지수 백오프를 적용합니다.
- 요청 기간·페이지·반환 건수에 상한을 둡니다.
- 서비스 키는 응답 또는 일반 로그에 노출하지 않습니다.

### FR-004 거래 정규화

- 거래 금액은 만 원 단위 정수로 저장합니다.
- 전세와 월세를 월세액으로 구분합니다.
- 계약일, 수집일, 취소일을 별도 필드로 둡니다.
- 원본 필드가 없는 값은 `null`이며 0으로 대체하지 않습니다.
- 안정적인 source record hash를 생성합니다.

### FR-005 취소·정정

- `cdealType`, 해제일 등 원천 필드를 이용해 취소 상태를 보존합니다.
- 취소 거래는 기본 통계에서 제외합니다.
- `include_canceled=true`일 때 원본과 함께 조회할 수 있습니다.

### FR-006 단지 검색과 스냅샷

- 지역·기간 내 실거래 단지명을 검색합니다.
- 동일 면적 기본 허용 오차는 ±1㎡입니다.
- 최신 매매·전세, 90일 중위값, 최고가, 회복률, 전세가율, 추정 갭, 거래량을 반환합니다.
- 표본 0건이면 계산 불가로 표시합니다.

### FR-007 단지 비교

- 2~10개 단지를 같은 기간·면적 오차·산식으로 비교합니다.
- 각 단지의 원천 지역 코드를 보존합니다.
- 표본 수와 품질 등급을 함께 반환합니다.

### FR-008 지역 시장 분석

- 최근 30일과 직전 30일 거래량과 중위가를 비교합니다.
- 분모가 0일 때 무한대 대신 `REOPENED` 또는 `NO_BASELINE`을 반환합니다.
- 거래량, 중위가, 회복률, 모멘텀, 전세가율, 추정 갭 순위를 지원합니다.

### FR-009 신호

- 신고가: 최신 거래가 그 이전 유효 거래의 최고가를 초과
- 거래 재개: 직전 유효 거래와 90일 이상 간격
- 신호는 결정론적 산식 버전과 근거 거래 ID를 가져야 합니다.
- “상승 확정”, “매수 신호”로 표현하지 않습니다.

### FR-010 청약·금융 호환

- vendored upstream 도구로 청약 공고·결과와 금융 계산 기능을 제공합니다.
- canonical 금융 계산기는 원리금균등·원금균등, 복리, 임대 현금흐름을 지원합니다.
- 금융 결과는 가정 기반 계산이며 승인·수익을 보장하지 않습니다.

### FR-011 관심 목록

- 로컬 모드에서 JSON 파일에 원자적으로 저장합니다.
- profile_id, 단지, 지역, 면적, 레이블을 지원합니다.
- 공개 HTTP 환경은 PostgreSQL·OAuth·RLS 어댑터를 사용하도록 경고합니다.
- 삭제는 결과의 `deleted` 값을 반환합니다.

### FR-012 출처와 최신성

모든 조회 응답은 다음을 포함합니다.

- `answered_at`
- `timezone`
- `sources`
- 조회한 `deal_months`
- 최신 신고 데이터라는 고지
- 취소·정정·신고 지연 가능성

### FR-013 호환 계층

- `src/real_estate/`를 동일 배포물에 포함합니다.
- `ENABLE_REAL_ESTATE_MCP_COMPAT`로 원본 도구 등록을 제어합니다.
- 호환 계층이 실패하더라도 canonical 서버가 기동 가능한 방향으로 격리합니다.
- 원본 MIT 저작권·허가 고지를 보존합니다.

## 9. 비기능 요구사항

| ID | 요구사항 |
|---|---|
| NFR-001 | Python 3.11·3.12 지원 |
| NFR-002 | stdio와 Streamable HTTP 지원 |
| NFR-003 | 순수 계산 모듈 80% 이상 단위 테스트 대상 |
| NFR-004 | API 키 로그·도구 결과 노출 0건 |
| NFR-005 | 동일 입력 fixture에 동일 지표 결과 |
| NFR-006 | 취소 거래 기본 통계 누출 0건 |
| NFR-007 | 단일 API 호출 timeout 기본 20초, 재시도 기본 3회 |
| NFR-008 | 한 요청 최대 60개월, 50페이지, 2,000건 반환 |
| NFR-009 | 로컬 관심 목록 저장 시 임시 파일 + atomic replace |
| NFR-010 | 제3자 라이선스 파일과 THIRD_PARTY_NOTICES 유지 |
| NFR-011 | Docker non-root 실행 |
| NFR-012 | GitHub Actions에서 lint, test, package validation 수행 |

## 10. 데이터 모델

canonical 런타임은 다음 핵심 필드를 사용합니다.

```text
Transaction
├─ source / source_record_id
├─ property_type / trade_type
├─ lawd_code / address fields
├─ contract_date / collected_at
├─ price / deposit / monthly_rent
├─ exclusive_area / land_area / floor / build_year
├─ deal_method
├─ is_canceled / canceled_at
└─ raw (선택적 출력)
```

운영형 저장 모델은 `database/schema.sql`과 `database/migrations/002_real_estate_mcp_integration.sql`을 사용합니다.

## 11. 안전·권한 요구사항

- 아파트Me는 기본 `LINK_OUT_ONLY`입니다.
- 민간 원천 활성화에는 승인 문서, 범위, 만료일, 캐시 정책이 필요합니다.
- 개인 식별자와 관심 목록은 공개 응답에 포함하지 않습니다.
- HTTP 다중 사용자 쓰기는 인증과 사용자 범위 격리 없이 허용하지 않습니다.
- 결과를 투자 권유, 감정평가, 법률·세무·대출 확정으로 표현하지 않습니다.

## 12. 수용 기준

1. 단일 `pip install -e .` 후 별도 real-estate-mcp 설치 없이 서버가 기동합니다.
2. `resolve_location("서울특별시 강남구")`가 11680 후보를 반환합니다.
3. fixture에서 취소 거래가 보존되고 기본 snapshot에서 제외됩니다.
4. 매매/전세 표본이 없으면 지표가 0이 아니라 null입니다.
5. 2개월 이상 API 응답은 페이지·월 단위로 합쳐지고 중복이 제거됩니다.
6. 아파트 외 자산 유형이 통합 거래 도구에서 선택됩니다.
7. 원본 호환 도구 등록을 환경 변수로 켜고 끌 수 있습니다.
8. MIT 제3자 고지가 패키지와 배포물에 포함됩니다.
9. `pytest`, `ruff`, `validate_package.py`가 통과합니다.
10. README의 stdio·HTTP·Docker 절차로 재현할 수 있습니다.

## 13. 단계

### v2.0

- 공공 API 런타임
- canonical 17개 도구
- vendored 호환 계층
- 로컬 관심 목록
- Docker·CI·테스트·라이선스 고지

### v2.1

- PostgreSQL 저장 어댑터 실제 구현
- API 응답 캐시와 rate-budget 관리
- 청약 도구 canonical namespace 래퍼
- 지역 코드 갱신 파이프라인

### v2.2

- OAuth 관심 목록
- 변경 이벤트와 일간 브리핑 스케줄러
- 전국 증분 수집 워커
- 지표 산식 버전 레지스트리 연동

### v3.0

- 승인된 파트너 어댑터
- 공간 데이터·교통·학군 보조 원천
- 운영 대시보드와 데이터 품질 콘솔

## 14. 성공 지표

- 최신성 필요한 질의에서 도구 호출률 100%
- 취소 거래 누출 0건
- 지표 golden fixture 통과율 100%
- 지역 코드 benchmark 정확도 95% 이상
- 원천·기준 시각 표시율 100%
- 근거 없는 투자 단정 0건
- 별도 upstream 설치 요구 0건
